/** Canonical public domain — used instead of window.location.origin so links
 * stay consistent regardless of which host (custom domain or *.vercel.app)
 * someone used to reach the dashboard. */
export const SITE_URL = 'https://autosdigital.com.br';
